/** @type {import('next').NextConfig} */
const nextConfig = {
    // experimental: {
    //     serverActions: true, // If using Server Actions
    //     webpack: (config) => {
    //       config.resolve.fallback = { fs: false, path: false }; // Fix dynamic require() issues
    //       return config;
    //     },
    //   },
};

export default nextConfig;
